source 'https://rubygems.org'

gem 'anthropic'       # Claude API
gem 'ruby-openai'     # OpenAI API
gem 'redis'          # Caching
gem 'pg'             # PostgreSQL
gem 'pgvector'       # Vector operations
gem 'dotenv'         # Environment variables
gem 'sinatra'        # Lightweight web server
gem 'puma'           # Web server
gem 'rack'           # Web server interface
gem 'nokogiri'
gem 'httparty'
gem 'sidekiq'        # Background job processing
gem 'connection_pool' # Connection pooling for Sidekiq
gem "rake"

# Admin Panel
gem 'activerecord', '~> 7.0.0'  # Pin to version 7
gem 'sinatra-activerecord', '~> 2.0.27'
gem 'sinatra-contrib', require: false
gem 'padrino-helpers'
gem 'will_paginate'
gem 'rack-cors'

group :development, :test do
  gem 'rspec'
  gem 'vcr'
  gem 'webmock'
  gem 'rubocop'
end
